//wap to find the largest among the two numbers 
#include<stdio.h>
int main(){

	int a,b;
	printf("enter the first number a :");
	scanf("%d", &a);
	printf("enter the second number b :");
	scanf("%d", &b);

	if(a>b) {
		 printf("the first number a is largest amd b is smallest\n" );
		}
	else 
             printf(" the second number b is largest and a is smallest\n");
	 
	return 0;
      }

